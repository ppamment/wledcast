# TODO: Give GPT the Mapper class and this yaml,
# and ask it to update the code to manage controllers,
# using a structure like: {
#   "controller_1": (shape_1, ..., shape_n),
#   "controller_2": (shape_1, ..., shape_n),
#   ...
# }
# and add the function to send pixels to the controllers
# using a structure like: {
#   "controller_1": (pixel_1, ..., pixel_n),
#   "controller_2": (pixel_1, ..., pixel_n),
#   ...
# }
controller:
  - ddp:
      host: wled-matrix.local
      id: controller_1  # TODO: name defaults to host
  - ddp:
      host: ring.local
      id: controller_2
  # - e131:
  #     host: example.com
  #     ...
  # - esphome:
  #     host: example.com
  # - beamer:
  #     ...

mapping:
  - translate:
      controller:
        id: controller_1
        # first: 1  # TODO: first number for pixels sequence ?
      items:
        # - matrix:
        #     width: 8
        #     height: 8
        #     firstled: topleft
        #     controller:
        #       id: controller_1
        - include:
            file: 'dev-mapping-multiring.yaml'
            controller:
              id: controller_2
        - translate:
            x: 17.6
            y: 0.8
            items:
              - include:
                  file: 'dev-mapping-clock.yaml'
                  controller:
                    id: controller_3
